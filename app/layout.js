export const metadata = { title:"project-forge", description:"Full TPT bundle creation platform — generates, formats, and uploads educational resource packages." };
export default function RootLayout({ children }) {
  return (<html lang="en"><body>{children}</body></html>);
}
